{
  "version": 2,
  "builds": [{ "src": "product.js", "use": "@vercel/node" }],
  "routes": [
    { "src": "/api/products", "dest": "product.js" },
    { "src": "/api(.*)", "dest": "server.js" }
  ]
}
